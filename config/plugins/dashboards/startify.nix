{
  vim.dashboard.startify = {
    enable = true;

    # TODO: add more bookmarks, see docs for more info (github and nvf search)
    bookmarks = [ 
      {
        c = "~/dotfiles";
      }
    ];
  };
}
